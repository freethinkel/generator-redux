import { combineReducers, Middleware } from 'redux';
import { reducer as formReducer } from 'redux-form';

/* REDUCERS */

/* MIDDLEWARES */

export const reducers = combineReducers({});

export const middlewares: Middleware[] | any = [];
